name: DB Fahrplan Alert

on:
  schedule:
    # Läuft zur vollen und halben Stunde zwischen 08:00 und 21:30
    - cron: "0,30 8-21 * * *"
    # Letzte Ausführung um 22:00
    - cron: "0 22 * * *"
  workflow_dispatch: # Manueller Start möglich

jobs:
  run-alert:
    runs-on: ubuntu-latest

    env:
      Telegram_BOT: ${{ secrets.TELEGRAM_BOT }}
      Telegram_CHAT: ${{ secrets.TELEGRAM_CHAT }}
      DB_API: ${{ secrets.DB_API }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup R environment
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.4.1'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev libxml2-dev libssl-dev

      - name: Install R package dependencies
        run: |
          Rscript -e 'install.packages(c("shiny", "dplyr", "lubridate", "httr", "xml2", "purrr", "stringr", "tibble"))'

      - name: Run Shiny script
        run: |
          Rscript app.R
